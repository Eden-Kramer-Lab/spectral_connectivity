version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current; one grouped PR per week.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      actions:
        patterns:
          - "*"
